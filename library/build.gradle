apply plugin: 'com.android.library'

android {
    compileSdkVersion 30
    buildToolsVersion "30.0.0"

    defaultConfig {
        minSdkVersion 19
        targetSdkVersion 30
        versionCode 1
        versionName "1.0"

        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
        consumerProguardFiles "consumer-rules.pro"
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
}

dependencies {
    implementation fileTree(dir: "libs", include: ["*.jar"])
    api 'androidx.core:core:1.3.2'
    api 'androidx.annotation:annotation:1.2.0'
    testImplementation 'junit:junit:4.13.2'
    androidTestImplementation 'androidx.test.ext:junit:1.1.2'
    androidTestImplementation 'androidx.test.espresso:espresso-core:3.3.0'
    api "com.contrarywind:wheelview:4.1.0"
    //api "com.contrarywind:Android-PickerView:4.1.9"
}


//apply plugin: 'bintray-release'
//添加
//publish {
//    repoName = "android"
//    artifactId = 'DatePicker'
//    userOrg = 'foolishchow'
//    groupId = 'me.foolishchow.android'
//    //uploadName = rootProject.uploadName
//    publishVersion = "0.0.2"//rootProject.publishVersion
//    desc = "android simple date picker base on Android-PickerView"
//    website = 'https://github.com/foolishchow/DatePicker.git'
//}


apply plugin: 'com.android.library'
apply plugin: 'com.github.dcendents.android-maven' //this way we can find the android-maven in jcenter
group='me.foolishchow.android'